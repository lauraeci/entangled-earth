<%= form_for accomplishment, html: { multipart: true } do |f| %>
  <% if accomplishment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(accomplishment.errors.count, "error") %> prohibited this accomplishment from being saved:</h2>

      <ul>
      <% accomplishment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.file_field :totem %>

  <div class="field">
    <%= f.label :i_accomplished %>
    <%= f.text_field :i_accomplished %>
  </div>

  <div class="field">
    <%= f.label :on %>
    <%= f.text_field :on %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

